# 未能确认或需后续深挖的内容

> 这些内容不是空白失败项，而是公开资料无法完全证明或本轮未展开到全量实体级别的部分。

## 闭源引擎层

Noita 的 C++ 层没有公开完整源码。以下函数的内部实现只能通过 Lua 调用边界、XML 组件和游戏表现推断：

- `BeginProjectile`
- `EndProjectile`
- `BeginTriggerTimer`
- `BeginTriggerHitWorld`
- `BeginTriggerDeath`
- `ConfigGunActionInfo_PassToGame`
- `SetProjectileConfigs`

本归档没有伪造这些函数的内部源码，只记录 Lua 可见逻辑。

## 全量 XML 尚未逐个展开

本轮缓存并分析了代表性实体 XML：火花弹、魔法箭、魔法弹、链锯、光明穿凿、黑洞、治疗魔弹、传送魔弹、炸弹等。422 个 action 关联的所有 XML 还没有全部下载并逐项解析。

如果后续要做到“每个法术都有实体级参数表”，下一步应从 `法术动作索引_422项.csv` 的 `related_projectiles` 字段批量拉取 XML，并解析 `ProjectileComponent`、`damage_by_type`、`config_explosion`、`LuaComponent`。

## 翻译缺口

自动提取发现 3 个 action 缺英文或中文本地化，详见 `自动提取缺口报告.md`：

- `FUNKY_SPELL`
- `METEOR_RAIN`
- `WORM_RAIN`

这三个 action 的底层逻辑仍在 `gun_actions.lua` 中存在，只是 `common.csv` 中本地化字段缺失或异常。

## 社区论坛资料

论坛和社区指南适合补充玩法术语，但同一组合技可能跨版本失效。本轮没有把单个 Reddit/Steam 帖子的说法写成机制结论。若要继续深挖，应优先把社区说法转化为可在 `gun.lua` / `gun_actions.lua` / XML 中验证的命题。

